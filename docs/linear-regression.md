# Linear Regression
There are different ways to implement this into dataset, matrix way, gradient descent way and special methods like lasso and ridge, We would craft all way around with all forms mixed unit using things from all types into one.

Goal : Find weights | Non-redundent | usable weights for testing
Making weights such that it could be used for predictions, with little optimization such that it could be scalled for more data points and people can use yantragya Linear regression module to make their modles.

GOAL : **Explore different modules of regression technique into working componenet**
