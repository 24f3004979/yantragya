# Transformation
Raw Dataset -> simplification with preserving original form
maping raw dataset to simpler space with less complexity and better perspective

## Simple ways
1. Normalization and centering
2. Aggregation with certain rules
3. Filterring with removing outliers
4. Binning continuous dataset for easy visualization

routing raw dataset for simple visualization and feature extraction, thus we require to simplify the dataset into simpler form which is easy to visualize and comprehend.

+ remove outliers
+ Normalization function
+ implement PCA for simplification
+ visualize the dataset
